<UserControl x:Class="ConfigModel.SingleDevice"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ConfigModel"
             mc:Ignorable="d" FontSize="15"
             >
    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader"  x:Key="DataGridHeaderStyleSample" >
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid Name="Root">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Rectangle x:Name="BackgroundRectangle" Stretch="Fill"  Fill="SkyBlue" Grid.ColumnSpan="2" Grid.RowSpan="2"/>
                            <ContentPresenter x:Name="HeaderText" Grid.RowSpan="2"       Content="{TemplateBinding Content}" Cursor="{TemplateBinding Cursor}"       HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"  Margin="{TemplateBinding Padding}"/>
                            <Rectangle Name="VerticalSeparator" Grid.RowSpan="2"      Grid.Column="2" Width="1" VerticalAlignment="Stretch"      Fill="Black"      Visibility="Visible" />

                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <DockPanel>
            <CheckBox Name="selectCheck" Margin="5" VerticalAlignment="Center" Checked="selectCheck_Checked" />
            <ComboBox Name="deviceType" MinWidth="100" Margin="5" SelectionChanged="deviceType_SelectionChanged" Height="40"/>
            <DataGrid Name="configInfo" Margin="5"  AutoGenerateColumns="False" CanUserAddRows="False" ColumnHeaderStyle="{StaticResource DataGridHeaderStyleSample}" ColumnHeaderHeight="30" MinColumnWidth="100" MinHeight="70">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="发送">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button VerticalAlignment="Center" Name="sendTest" Content="测试" HorizontalAlignment="Center" Margin="0,1,0,1"
                                  Height="30" Width="50" Click="SendTest_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="回复信息" Binding="{Binding AckData}"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>   
    </Grid>
</UserControl>
