<UserControl x:Class="UIRealTimeStatus.RealTimeStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UIRealTimeStatus"
             mc:Ignorable="d" Loaded="UserControl_Loaded" FontFamily="Arial,SimSun" 
            >
    <!--UserControl.Resources>
        <Style x:Key="statusItemStyle" TargetType="{x:Type ListViewItem}">
            <Setter Property="Background">
                <Setter.Value>
                    <Binding RelativeSource="{RelativeSource Self}" 
                        Converter="{StaticResource StatusConvert}"/>
                </Setter.Value>
            </Setter>
        </Style>
    </-->
    <Grid>
        <GroupBox >
            <ListView Name="listStatus" ItemsSource="{Binding}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="序列号" Width="50" DisplayMemberBinding="{Binding Index}"/>
                        <GridViewColumn Header="提示信息" Width="300" DisplayMemberBinding="{Binding Status}"/>
                        <GridViewColumn Header="时间" Width="100" DisplayMemberBinding="{Binding StatusTime}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>  
    </Grid>
</UserControl>
